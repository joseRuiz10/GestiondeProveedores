@model IEnumerable<TFIGestionProveedores03.Calificacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.calificacion1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.calificacion1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCalificacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCalificacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCalificacion })
        </td>
    </tr>
}

</table>
